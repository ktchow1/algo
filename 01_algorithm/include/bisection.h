#pragma once
#include<vector>
#include<functional>
#include<optional>

/*
namespace alg
{
    inline std::optional<double> bisection(std::function<double(double)>& fct, double x0, double x1)
    {
        // 1. check edge case, or init y
        double y0 = fct(x0);
        double y1 = fct(x1);

        // 2. check solution existence
        if (x0 * x1 > 0) return std::nullopt;

        // 3. check stop condition


        // 4. mid point
        
        // 5. bisection
        
        // 6. answer



    }

    inline double bisection(const std::function<double(double)>& fct, double x0, double x1)
    {
    }
}

namespace alg
{
    inline std::optional<std::size_t> bisection(const std::vector<std::int64_t>& vec, std::int64_t target)
    {
    }

    inline std::optional<std::size_t> bisection_impl(const std::vector<std::int64_t>& vec, std::int64_t target, std::size_t x0, std::size_t x1)
    {
    }
}

namespace alg
{
    inline std::optional<std::size_t> mode_bisection(const std::vector<std::int64_t>& vec)
    {
    }
}

namespace alg
{
    inline std::optional<std::size_t> rotated_bisection(const std::vector<std::int64_t>& vec, std::int64_t target)
    {
    }
}

*/
